\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}


\begin{document}
	
	% IMPORT-DATA recomp ../results/bench_recomp.txt
	
	\begin{tabular}{l|r|r|r|r|r|r}
		Datensatz/Zeit (s) & Größe (MB) & Seq\_PComp & Par\_PComp (4 Threads) & full parallel & fast\_PComp & naive\_PComp \\ \hline
		%% TABULAR REFORMAT(row 0-19=(min=bold))
		%% SELECT s.dataset AS x,
		%% (SELECT (text / 1000000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "fast_bcomp" AND s1.level = 0 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "seq_pcomp" AND level = 1 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "parallel_pcomp" AND level = 1 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "full_parallel_pcomp" AND level = 1 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "fast_pcomp" AND level = 1 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "naive_pcomp" AND level = 1 GROUP BY s1.dataset)
		%% FROM recomp s GROUP BY s.dataset
  Escherichia\_Coli & 112 & 14 &  7 & 11 &  \textbf{3} &  6 \\
          coreutils & 205 & 40 & 17 & 24 & \textbf{12} & 40 \\
      dblp.xml.50MB &  52 & 10 &  4 &  6 &  \textbf{2} &  8 \\
                dna & 403 & 56 & 29 & 44 & \textbf{12} & 26 \\
           dna.50MB &  52 &  6 &  3 &  4 &  \textbf{1} &  3 \\
    einstein.de.txt &  92 & 16 &  7 & 11 &  \textbf{5} & 14 \\
             kernel & 257 & 48 & 22 & 32 & \textbf{16} & 48 \\
            sources & 210 & 40 & 18 & 25 & \textbf{13} & 40 \\
     world\_leaders &  46 &  5 &  2 &  3 &  \textbf{1} &  3 \\
  % END TABULAR SELECT s.dataset AS x, (SELECT (text / 1000000) FROM recomp s1...)
	\end{tabular}
\end{document}
