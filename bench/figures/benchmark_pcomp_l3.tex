\documentclass[10pt,a4paper]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}

\usepackage{tikz}
\usepackage{pgfplots}


\begin{document}
	
	% IMPORT-DATA recomp ../results/bench_recomp.txt
	
	\begin{tabular}{l|r|r|r|r|r|r}
		Datensatz/Zeit (s) & Größe (MB) & Seq\_PComp & Par\_PComp (4 Threads) & full parallel & fast\_PComp & naive\_PComp \\ \hline
		%% TABULAR REFORMAT(row 0-19=(min=bold))
		%% SELECT s.dataset AS x,
		%% (SELECT (text / 1000000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "fast_bcomp" AND s1.level = 0 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "seq_pcomp" AND level = 3 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "parallel_pcomp" AND level = 3 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "full_parallel_pcomp" AND level = 3 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "fast_pcomp" AND level = 3 GROUP BY s1.dataset),
		%% (SELECT ((SUM(time) / 5) / 1000) FROM recomp s1 WHERE s1.dataset = s.dataset AND s1.algo = "naive_pcomp" AND level = 3 GROUP BY s1.dataset)
		%% FROM recomp s GROUP BY s.dataset
  Escherichia\_Coli & 112 & 10 &  \textbf{4} &  6 & \textbf{4} & 12 \\
          coreutils & 205 & 29 & \textbf{13} & 18 &         18 & 60 \\
      dblp.xml.50MB &  52 &  7 &  \textbf{3} &  4 & \textbf{3} & 12 \\
                dna & 403 & 38 & \textbf{16} & 23 &         20 & 54 \\
           dna.50MB &  52 &  5 &  \textbf{2} &  3 & \textbf{2} &  6 \\
    einstein.de.txt &  92 & 12 &  \textbf{5} &  7 &          6 & 19 \\
             kernel & 257 & 37 & \textbf{16} & 22 &         22 & 71 \\
            sources & 210 & 33 & \textbf{14} & 19 &         21 & 63 \\
     world\_leaders &  46 &  3 &  \textbf{1} &  2 & \textbf{1} &  5 \\
  % END TABULAR SELECT s.dataset AS x, (SELECT (text / 1000000) FROM recomp s1...)
	\end{tabular}
\end{document}
